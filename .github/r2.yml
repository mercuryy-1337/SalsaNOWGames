name: Build and Deploy to R2

on:
  push:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.3

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v1

      - name: Restore NuGet packages
        run: nuget restore SalsaNOWGames.sln

      - name: Build Release
        run: msbuild SalsaNOWGames.sln /p:Configuration=Release /p:Platform="Any CPU"

      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: SalsaNOWGames
          path: SalsaNOWGames/bin/Release/SalsaNOWGames.exe

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Wrangler
        run: npm install -g wrangler@latest

      - name: Upload to R2
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_R2_TOKEN }}
        run: |
          wrangler r2 object put gfn/SalsaNOWGames.exe --file ./SalsaNOWGames/bin/Release/SalsaNOWGames.exe --remote
